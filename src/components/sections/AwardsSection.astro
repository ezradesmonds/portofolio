---
import { awards } from "../../data/awards";
import SectionHeading from "../ui/SectionHeading.astro";
---

<section id="awards" class="section bg-dark-surface/30">
  <div class="container-lg">
    <SectionHeading
      number="03."
      title="Awards & Achievements"
    />

    <div class="grid sm:grid-cols-2 lg:grid-cols-4 gap-4 sm:gap-6">
      {awards.map((award) => (
        <div class="glass-card rounded-xl p-4 sm:p-6 border-glow">
          <div class="text-2xl sm:text-3xl font-bold text-accent-cyan font-mono mb-1 sm:mb-2">{award.title}</div>
          <div class="text-[10px] sm:text-xs font-mono text-text-muted uppercase tracking-wider mb-1">{award.year}</div>
          <h3 class="text-xs sm:text-sm font-semibold text-white mb-2 sm:mb-3">{award.event}</h3>
          <p class="text-[11px] sm:text-xs text-text-secondary leading-relaxed">{award.description}</p>
        </div>
      ))}
    </div>
  </div>
</section>

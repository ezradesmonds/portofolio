---
import { resolveLang, t } from "../../i18n/utils";
import { awards } from "../../data/awards";
import { localizeAwards } from "../../i18n/content";
import SectionHeading from "../ui/SectionHeading.astro";
import GlowAwardCard from "../interactive/GlowAwardCard";

const lang = resolveLang(Astro.url.searchParams.get("lang"), Astro.url.pathname);
const localizedAwards = localizeAwards(awards, lang);
---

<section id="awards" class="section bg-dark-surface/30">
  <div class="container-lg">
    <SectionHeading
      number="04."
      title={t(lang, "awards.title")}
    />

    <div class="grid sm:grid-cols-2 lg:grid-cols-4 gap-4 sm:gap-6">
      {localizedAwards.map((award) => (
        <GlowAwardCard award={award} client:load />
      ))}
    </div>
  </div>
</section>
